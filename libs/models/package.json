{
  "name": "@medea/models",
  "version": "0.0.0",
  "description": "schemas & interfaces for server and client",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "rm -rf dist && tsup src/index.ts --dts --minify --watch --sourcemap",
    "build": "tsup src/index.ts --dts --minify",
    "lint": "eslint --fix src/**",
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sinclair/typebox": "^0.32.0"
  },
  "devDependencies": {
    "tsup": "^5.2.1"
  }
}
